// GENERATED from packages/auth (the Better Auth config + plugins) — do not hand-edit.
// From the repo root:
//   pnpm db:generate   # writes this file
//   pnpm db:migrate    # creates + applies the SQL migration to your Neon database
//
// Until you run db:generate, this stub keeps the workspace type-checking.
// Future domain tables (memory atoms, trust/audit, …) get added to their own
// files under this package as those domains are built.
export {};
