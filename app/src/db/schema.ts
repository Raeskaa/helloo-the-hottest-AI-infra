// The auth tables (user, session, account, verification, organization, member,
// invitation, …) are GENERATED from src/auth.ts by the Better Auth CLI — do not
// hand-edit. Run:
//
//   npm run db:generate     # writes this file from the auth config + plugins
//   npm run db:migrate:gen  # creates the SQL migration in ./drizzle
//   npm run db:migrate      # applies it to your Neon database
//
// Until you run db:generate, this stub keeps the project type-checking.
export {};
