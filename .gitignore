# SECRETS — never commit (repo is public)
.dev.vars
.env
.env.*
*.local

# deps / build / caches
node_modules/
.wrangler/
.turbo/
dist/
worker-configuration.d.ts

# Next.js / OpenNext build artifacts (apps/web)
.next/
.open-next/
next-env.d.ts
cloudflare-env.d.ts
*.tsbuildinfo
